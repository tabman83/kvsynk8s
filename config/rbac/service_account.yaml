apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/name: kvsynk8s
    app.kubernetes.io/managed-by: kustomize
  annotations:
    # Real value supplied at install time (see quickstart.md Azure setup):
    # the client ID of the user-assigned managed identity federated with
    # this ServiceAccount for Microsoft Entra Workload ID.
    azure.workload.identity/client-id: "<SET-ME>"
  name: controller-manager
  namespace: system
